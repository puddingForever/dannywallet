<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.danny.dannywallet.mapper.UserMapper">

	<!-- 회원리스트 -->
	<select id="selectUserList" resultType="com.danny.dannywallet.domain.UserVO">
		SELECT * 
		  FROM danny.users_table
	  ORDER BY user_regDate DESC
	</select>

	<!-- 회원가입 -->
	<insert id="insertUser" parameterType="com.danny.dannywallet.domain.UserVO">
	<selectKey keyProperty="user_idx" order="BEFORE">
		SELECT danny.user_seq.nextval from dual
	</selectKey>
		INSERT INTO users_table
		VALUES (#{user_idx},#{user_id},#{user_password},#{user_regDate})
	</insert>
	
	<!-- 회원탈퇴  -->
	<delete id="deleteUser" parameterType="com.danny.dannywallet.domain.UserVO">
		DELETE danny.users_table
		 WHERE user_idx = #{user_idx}
	</delete>
	
	<!-- 로그인  -->
	<select id="selectLoginUserInfo" parameterType="com.danny.dannywallet.domain.UserVO" 
							resultType="com.danny.dannywallet.domain.UserVO">
		SELECT user_idx,user_name 
		  FROM danny.users_table
         WHERE user_id = #{user_id} AND user_password = #{user_password}
	</select>
	
	<!-- 아이디 중복 체크 -->
	<select id="checkUserIdExist" parameterType="String" resultType="String">
		SELECT (case when count(*) = 1 then 'unusable' else 'usable' end)AS RESULT
		  FROM danny.users_table 
		 WHERE user_id = #{user_id}
	</select>
	
	
</mapper>